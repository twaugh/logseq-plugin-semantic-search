{
  "name": "logseq-plugin-semantic-search",
  "version": "0.1.0",
  "description": "Search your Logseq notes by meaning, not just keywords",
  "main": "dist/index.html",
  "scripts": {
    "dev": "vite",
    "build:icon": "node scripts/build-icon.mjs",
    "build": "npm run build:icon && tsc && vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@logseq/libs": "^0.0.17"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "sharp": "^0.34.5",
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "logseq": {
    "id": "logseq-plugin-semantic-search",
    "title": "Semantic Search",
    "icon": "./icon.png"
  }
}
